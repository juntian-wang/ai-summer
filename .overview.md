# 🎙️ AI 奇葩说 — 前端开发完成

## 文件
- **`index.html`** — 774 行单文件 HTML，纯前端无框架

## 已实现的功能

### 阵容配置区
- 辩题下拉 + 自定义输入
- 6 位辩手风格选择（正/反方各一/二/三辩）
- 主持人 + 导师选择
- 数据从 `data/*.json` 自动加载，无后端依赖

### 辩论播放核心
- `playLoop` 异步循环，逐步骤播放
- 打字机逐字效果（50ms/字），支持暂停/恢复
- 6 种渲染函数：host / speech / battle / vote / sway / mentor

### 视觉组件
- **左右气泡**：正方左对齐（紫色边框），反方右对齐（橙色边框）
- **开杠对战面板**：左右分栏，当前发言者高亮放大
- **投票条形图**：CSS 过渡动画，初投→终投平滑过渡
- **跑票卡片**：含观众标签 + 方向 + 原因
- **导师卡片**：含名称 + 个人简介

### 侧边栏
- **情绪面板**：基于 audience.json 的实时情绪引擎，演讲时自动分析文本特征（幽默/逻辑/情感/金句/故事）→ 更新 30 位观众的情绪分布 → 显示 😂🤔😤👏😐
- **跑票追踪**：实时显示正方票数的变化序列

### 控制
- ⏸️ 暂停 / ▶️ 继续（停止 fetch + 停止打字机）
- ⏭️ 跳过当前发言
- ✅ 导出台本（下载 .txt）
- ✅ 再来一局

### 异常处理
- 数据加载失败自动降级为内嵌数据
- 状态栏显示"⏳ 正在生成..." / "⏸️ 已暂停"
- Toast 消息提示

### 响应式
- 桌面端（1100px 宽，侧边栏在右）
- 移动端（<768px：侧边栏移至下方，自适应宽度）

## 使用方式
1. 启动本地服务器：`python3 -m http.server 3000`
2. 打开 `http://localhost:3000`
3. 选择辩题和阵容，点击「开始辩论」
4. 观看完整辩论 + 实时情绪 + 跑票追踪

## 未完成（待联调）
- 对接后端 API（任务 C 完成后）
- 刷新恢复 session 状态
- V2 额外效果（奇袭、导师插话、金句高亮等）
