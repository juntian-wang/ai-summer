# AI 奇葩说 — Loop 设计

> 我不是在手动写 prompt。我设计了一个 Loop，它自己驱动整场辩论。

---

## 1. Loop 的目标

**产出一集完整的辩论节目，包含：**

- [ ] 主持人开场 + 串联（全部 6 个串场节点完成）
- [ ] 6 位辩手按序发言（每人 300-400 字，风格符合 Profile）
- [ ] 6 轮自由开杠（每轮 50-150 字，有效回应对方上一轮）
- [ ] 2 段结辩（反方先，正方收尾，200-300 字）
- [ ] 30 位观众初投 + 终投（有效票数 ≥ 20/30）
- [ ] 至少出现跑票（若跑票为 0，触发"加赛开杠"）
- [ ] 2 位导师轮流点评（每人 150-300 字）

**一集辩论 = 一次 Loop 执行完成。**

---

## 2. Loop 的执行过程

```
┌──────────────────────────────────────────────────────────┐
│                    LOOP 主循环                             │
│                                                          │
│  for (step of FLOW) {                                     │
│    ┌───────────────────────────────────────────────��─┐   │
│    │ ① 读 FLOW，确定当前环节类型                       │   │
│    │    host | speech | battle | closing | vote | mentor  │
│    └─────────────────────────────────────────────────┘   │
│                         ↓                                 │
│    ┌─────────────────────────────────────────────────┐   │
│    │ ② 构建 Prompt                                    │   │
│    │    = System Profile（该角色的 Skills）             │   │
│    │    + Session Memory（前面谁说过了什么）             │   │
│    │    + Step Task（当前环节要做什么）                  │   │
│    └─────────────────────────────────────────────────┘   │
│                         ↓                                 │
│    ┌─────────────────────────────────────────────────┐   │
│    │ ③ 调用 LLM（DeepSeek API）                       │   │
│    │    带重试逻辑（见 §4）                            │   │
│    └─────────────────────────────────────────────────┘   │
│                         ↓                                 │
│    ┌─────────────────────────────────────────────────┐   │
│    │ ④ 验证输出（见 §3）                              │   │
│    │    通过 → 存入 Memory → 推前端 → 下一步           │   │
│    │    失败 → 触发降级路径 → 重试或跳过                │   │
│    └─────────────────────────────────────────────────┘   │
│  }                                                        │
│                                                          │
│  全部步骤完成 → ⑤ 终态验证（跑票检查）                     │
│    跑票 = 0 → 触发一轮加赛开杠 → 回到开杠步骤             │
│    跑票 > 0 → Loop 成功终止                               │
└──────────────────────────────────────────────────────────┘
```

### 各步骤类型的处理细节

| 步骤类型 | 调 LLM | 内存写入 | 前端即时输出 |
|---------|--------|---------|------------|
| `host` | 是 | 追加到 memory | 主持词（居中斜体） |
| `speech` | 是 | 追加到 memory | 辩手气泡 + 微反应更新 |
| `battle` | 是 | 追加到 memory | 对战面板 + 微反应更新 |
| `closing` | 是 | 追加到 memory | 结辩气泡 |
| `vote` | 是（30 次分批） | 不写入 memory（单独存） | 投票条形图 |
| `mentor` | 是 | 追加到 memory | 导师卡片 |

### 观众投票子循环（`vote` 步骤内部）

```
for (batch of 5 viewers) {
  await Promise.all(batch.map(v => callLLM(votePrompt(v))))
  if (failCount > 0) → 记录失败观众
  if (batch 不是最后一批) → sleep 500ms
}

所有批次完成 → 统计有效票数 → 输出投票结果
```

---

## 3. Loop 的自我检查

### 3.1 每步生成后立即检查

| 检查项 | 怎么验 | 不通过怎么办 |
|--------|--------|------------|
| **非空** | `output.length > 0` | 重试 |
| **长度合理** | 辩手 100-600 字，开杠 20-200 字，结辩 100-400 字 | 过长截断 + 标记；过短重试 |
| **不包含角色标记** | 不含 `[正方]` `【反方】` `辩手：` 等 | 清洗后使用（不重试） |
| **不包含拒绝回答** | 不含"抱歉""我不能""作为 AI" | 重试（说明它没理解角色） |
| **发言者身份正确** | battle 步骤检查输出没有"我觉得正方说的对"（反方不能说这种话） | 重试 |

### 3.2 关键节点的深度检查

| 节点 | 检查内容 | 不通过怎么办 |
|------|---------|------------|
| **初投** | 有效票 ≥ 20/30 | 不足 → 补默认票至 20 |
| **开杠 × 6 轮** | 每轮是否回应了上一轮的关键词（至少出现对方刚说的 1-2 个关键词） | 不通过 → 重试 |
| **终投** | 有效票 ≥ 20/30 | 不足 → 补默认票 + 标注"有效票 X/30" |
| **终投 vs 初投** | 跑票人数 > 0？ | 跑票 = 0 → 触发"加赛开杠"（1 轮）→ 再投 |

### 3.3 终态验证：跑票检查

这是 Loop 最重要的自检——**一场辩论如果没有任何人跑票，说明辩论没起作用。**

```
终投完成后：
  if (跑票人数 === 0) {
    主持人（自动）："看来大家都很坚定啊——加赛一轮开杠！"
    执行 1 轮额外开杠（正方→反方）
    重新终投
    
    if (跑票人数仍然 === 0) {
      标注："本轮辩论未能改变任何观众的看法"
      Loop 正常结束（不算失败，但记录这个结果）
    }
  }
```

---

## 4. 停止条件

### 4.1 成功终止

```
FLOW 数组全部执行完成
  AND 终投有效票 ≥ 20
  AND 跑票检查完成（含可能触发的加赛）
→ Loop 状态 = SUCCESS
→ 前端显示 [再来一局] [导出台本]
```

### 4.2 降级终止

每步发言最多 4 次尝试：

```
尝试 1：完整 Prompt（风格 + 上下文 + 任务）
  ↓ 失败
尝试 2：完整 Prompt（等待 2s）
  ↓ 失败
尝试 3：完整 Prompt（等待 2s）
  ↓ 失败
尝试 4：简化 Prompt（去掉风格约束，只留辩题 + 立场 + 环节说明）
  ↓ 成功 → 继续下一步
  ↓ 失败 → 生成占位文本："（辩论暂时中断...）"
          前端显示 [跳过此发言] 按钮
          用户点击跳过 → 继续下一步（不算 Loop 失败）
```

**关键原则：单步失败不终止整个 Loop。** 一场辩论可以跳过一个发言继续，但不能跳过开杠、结辩和投票。

### 4.3 致命终止

| 触发条件 | 行为 |
|---------|------|
| 连续 3 个 speech 步骤全部降级失败（跳过） | Loop ABORT → "辩论无法继续，请重试" |
| 开杠或结辩步骤降级失败 | Loop ABORT（这些步骤不能跳过） |
| 终投有效票 < 10 | Loop ABORT → "观众参与不足，辩论无效" |
| DeepSeek API 连续 10 次返回 5xx | Loop ABORT → "服务不可用" |
| session 超时（30 分钟无前端请求） | Loop ABORT → 自动清理 |

### 4.4 暂停（非终止）

```
用户点击暂停 → 前端停止 fetch → 后端 session 保持原位
用户点击继续 → 前端恢复 fetch → 后端从 stepIdx 继续
用户关闭页面 → 30 分钟后 session 自动清理
```

---

## 5. Loop 的输出物

每次 Loop 成功执行后，产出：

1. **前端可播放的辩论节目**（HTML 页面实时渲染）
2. **完整台本**（`GET /api/session/:id/full` 返回 JSON）
3. **投票数据**（初投/终投/跑票明细）
4. **session 日志**（每步耗时、重试次数、降级触发情况 → 用于后续优化 Prompt）
