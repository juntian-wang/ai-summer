# 任务 D：前端界面搭建

> 独立任务。可交给任意对话执行。

---

## 目标

搭建完整的辩论节目前端页面，包含阵容配置、辩论播放、情绪面板、跑票曲线、动画效果。

产出 `index.html`（单文件，无框架依赖）。

---

## 前置依赖

- 任务 C 完成（需要后端 API 接口，但可先用 mock 数据独立开发）
- 参考文档：`要达到的效果.md`、`Loop设计.md`

---

## 技术栈

- 纯 HTML + CSS + JS（无 React/Vue 等框架）
- Chart.js（仅用于投票条形图，CDN 加载）

---

## 页面结构

```
┌──────────────────────────────────────┐
│  🎙️ AI 奇葩说                         │
├──────────────────────────────────────┤
│  阵容配置区                            │
│  ├─ 辩题选择（下拉 + 自定义输入）       │
│  ├─ 正方一/二/三辩 + 反方一/二/三辩     │
│  ├─ 主持人 + 导师选择                  │
│  └─ [开始辩论] 按钮                    │
├──────────────────────────────────────┤
│  辩论展示区（开始后显示）               │
│  ├─ ⏸️ 暂停/▶️ 继续 按钮               │
│  ├─ 主持人串场词（居中斜体）            │
│  ├─ 辩手发言气泡（左正右反）            │
│  ├─ 开杠对战面板（左右交替高亮）         │
│  ├─ 结辩气泡                           │
│  ├─ 投票条形图（初投/终投 + 动画过渡）   │
│  ├─ 跑票理由卡片                       │
│  └─ 导师点评卡片                       │
├──────────────────────────────────────┤
│  ┌──── 侧边栏 ────┐                    │
│  │ 😂▏▏ 7人      │                    │
│  │ 🤔▏▏▏▏▏ 5人  │                    │
│  │ 👏▏▏▏ 8人     │                    │
│  │ 😐▏▏▏▏ 10人   │                    │
│  │                │                    │
│  │ 📍实时跑票      │                    │
│  │ 正方 12→13→... │                    │
│  └────────────────┘                    │
├──────────────────────────────────────┤
│  Toast 提示 / 状态栏                   │
└──────────────────────────────────────┘
```

---

## 具体步骤

### Step 1：骨架 HTML + CSS

- 深色主题配色（深蓝紫底色 + 亮色文字）
- 响应式布局（桌面端宽 900px 居中，移动端自适应）
- 字体：系统中文默认字体（PingFang SC / Microsoft YaHei）

### Step 2：阵容配置区

- 加载 `data/debaters.json`、`data/topics.json`、`data/referee.json`、`data/mentors.json`
- 辩题下拉框 + 自定义输入框
- 6 个辩手风格下拉（正方一/二/三 + 反方一/二/三）
- 主持人下拉（1 个）+ 导师下拉（2 个固定标签，非多选）
- [开始辩论] 按钮
- 验证：不能全是同一种风格吗？V1 不限制，但给一个微笑提示

### Step 3：辩论播放核心循环

```javascript
async function playLoop(sessionId) {
  if (paused) return setTimeout(() => playLoop(sessionId), 500);
  
  const res = await fetch(`/api/session/${sessionId}/next`);
  const data = await res.json();
  
  if (data.done) { showActions(); return; }
  
  renderStep(data);  // 根据 type 渲染不同组件
  setTimeout(() => playLoop(sessionId), 800);
}
```

### Step 4：各步骤渲染函数

- `renderHost(text)` — 居中斜体，淡入动画
- `renderSpeech(data)` — 打字机逐字效果（setInterval 50ms/字），左正右反
- `renderBattle(data)` — 左右对战面板，当前位置高亮放大
- `renderVote(data)` — 条形图动画（初投→终投平滑过渡）
- `renderSway(data)` — 跑票卡片，含观众标签 + 原因
- `renderMentor(data)` — 导师卡片，含名称 + 头像图标

### Step 5：暂停/恢复

- 顶部固定栏 [⏸️ 暂停] 按钮
- 暂停时停止 fetch + 停止打字机
- 继续时恢复

### Step 6：情绪面板

实现 V1 前端微反应引擎（见 `制作步骤.md` 情绪引擎算法）：

- 10 个形态特征提取函数
- 30 观众 × 10 敏感度 × 特征值 → 情绪分数 → Emoji
- 侧边栏实时更新情绪分布条

### Step 7：跑票曲线

- 侧边栏显示实时的"正方 12 → 13 → ..."跑票线
- 数据来源于情绪引擎的 leaning 计算

### Step 8：异常处理 UI

- 加载中：状态栏"⏳ 正在生成..."
- LLM 失败：Toast "正在重试..."
- 跳过按钮：当发言失败时显示
- 刷新恢复：调用 `/api/session/:id/state` 恢复进度
- 网络断开：暂停 + 提示

### Step 9：结束操作

- [再来一局] — 刷新页面
- [导出台本] — 调 `/api/session/:id/full` → 下载 .txt

---

## 你的 Loop 自检

你是 Agent 5，遵循 Agent Loop 协议（见 `项目总览.md`）。

### 每完成一个 UI 组件后立即查

- [ ] 文件 `index.html` 已写入磁盘
- [ ] HTML 在浏览器能正常打开（`open index.html`，不白屏）
- [ ] 浏览器 console 无红色错误
- [ ] 组件在页面上可见（不是隐藏的、不是错位的）

### 核心检查项

- [ ] 选择辩题 + 配置阵容 → 点击开始 → 辩论逐幕播放（可用 mock 数据测试）
- [ ] 打字机效果流畅（setInterval 间隔合理，不卡顿）
- [ ] 辩手气泡左右交替（正方左、反方右）
- [ ] ⏸️ 暂停后点击 ▶️ 继续，流程恢复
- [ ] 情绪面板数值变化有实际依据（不是假数据）
- [ ] 跑票曲线数字随情绪变化
- [ ] 投票条形图有动画过渡
- [ ] 跑票卡片正确渲染
- [ ] 页面刷新后 `session/state` 能恢复进度
- [ ] 单个 HTML 文件即可打开（不需要 `npm install`）

### 自检失败怎么办

```
白屏/报错 → 打开 DevTools Console，修 JS 错误
组件不可见 → 检查 CSS display/visibility
功能不正常 → 加 console.log，定位，修复
同一问题连续 3 次修不好 → 标注为"待定"，继续下一个组件
```

### 停止条件

- **成功**：所有组件完成 + 页面功能正常
- **致命**：index.html 在浏览器白屏且无法修复 → 停止
- **降级**：部分非关键 UI（如 Toast 样式细节）留到联调 → 记录，仍算通过

### 终检

- [ ] 完整 mock 一集辩论流程，所有组件依次渲染
- [ ] 在 Chrome 和 Safari 各测一次
- [ ] 暂停/恢复/刷新 三个操作各测一次
- [ ] 文件大小 < 1000 行（保持可维护性）
