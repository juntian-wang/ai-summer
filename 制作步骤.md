# AI 奇葩说 — 制作步骤

> 从零到成品，每一步做什么、需要什么、产出什么。

---

## 角色总览

| 角色 | 人数 | 职责 |
|------|------|------|
| **主持人** | 1 个 | 控场——宣布辩题、介绍选手、推进环节、维持节奏 |
| **正方辩手** | 3 个 | 一辩立论、二辩补充、三辩深化 + **兼任结辩** |
| **反方辩手** | 3 个 | 同上（三辩兼任结辩） |
| **导师** | 2 个（固定） | 赛后点评、聊感受，**不打分** |
| **AI 观众** | 30 个 | 各有独立的判断维度、情感倾向、投票偏好，投票定胜负 |

---

## 完整辩论流程（V1）

```
🎬 【开场】主持人宣布辩题、介绍双方阵容
    ↓
📊 【初始投票】30 个观众根据自己的人设 + 辩题投第一轮（调 LLM）
    ↓
🎤【正方一辩】立论发言（约 300-400 字）
🎤【反方一辩】立论发言（约 300-400 字）
🎤【正方二辩】补充/反驳（约 300-400 字）
🎤【反方二辩】补充/反驳（约 300-400 字）
🎤【正方三辩】攻防深化（约 300-400 字）
🎤【反方三辩】攻防深化（约 300-400 字）
    ↓
⚔️【自由开杠】双方交替发言，每方 3 轮（共 6 段，每段 50-150 字）
    ↓
🏁【反方结辩】约 200-300 字，由反方三辩执行（反方先结）
🏁【正方结辩】约 200-300 字，由正方三辩执行（正方拿最后一句）
    ↓
📊 【最终投票】30 个观众重投 + 跑票结果 + 跑票理由（调 LLM）
    ↓
🎙️【导师点评】2 位导师轮流点评（不打分）
```

### 为什么结辩反方先、正方后？

辩论赛的结辩顺序是**先发言的先结**（正方先开始、正方最后收束），确保正方有"最后一句话"的优势。反方先结辩 → 正方结辩收尾。

---

## 功能版本规划

| 功能 | V1 | V2 |
|------|-----|-----|
| 6 人车轮战 + 开杠 + 结辩 | ✅ | 已有 |
| 主持人串场 | ✅ | 已有 |
| 双层情绪面板（微反应 + 深度分析） | ✅ | 已有 |
| 实时跑票追踪 | ✅ | 已有 |
| 导师赛后点评 | ✅ | 已有 |
| **奇袭机制**（辩手中途点名 1v1） | ❌ | ✅ |
| **导师中途插话** | ❌ | ✅ |
| **辩手策略选择**（攻击/防守/转移等） | ❌ | ✅ |
| **金句自动高亮**（LLM 标记 + 前端渲染） | ❌ | ✅ |
| 风格校准（音频学习） | ❌ | ✅ |
| TTS 语音播客版 | ❌ | ✅ |

---

## 技术架构总览

```
前端：index.html（纯 HTML+CSS+JS，无框架）
  │ fetch /api/*
  ▼
后端：Node.js + Express（~200 行 server.js）
  │ 状态机 + Prompt 工厂 + 共享记忆
  ▼
LLM：DeepSeek API（OpenAI 兼容格式）
```

**为什么选 DeepSeek**：中文辩论能力强、API 便宜、格式兼容 OpenAI（一行代码切换其他模型）。

**部署**：V1 本地 `localhost:3000`。需要公开时再上云服务器。

---

## 多 Agent 架构：编排器 + 无状态 Agent

所有 Agent 共享一个 LLM，通过切换 system prompt 扮演不同角色。Agent 自身**无记忆、无状态**——每次发言时，编排器把完整上下文（前面所有人说了什么）塞进 Prompt。

关键设计：
- **共享记忆**：一个数组存储所有发言，每段新发言往里追加
- **状态机**：FLOW 数组定义流程步骤，按序推进
- **Prompt 工厂**：根据角色类型 + 风格 Profile + 当前环节，组装不同的 system prompt
- **评委 30 人**：Normal 阶段纯 JS 对象（存 leanin/人设数据），只在初投和终投时各调一次 LLM（每次 30 个请求分批并行发出，每批 5 个）

```
多 Agent 代码结构（核心逻辑）：

FLOW = [开场, 初投, 正方一辩, 反方一辩, ..., 终投, 导师点评]
memory = []  // 共享记忆

for each step in FLOW:
    if step == "观众投票":
        30 viewers × 分批并行 LLM 调用
    else if step == "辩手发言":
        prompt = buildPrompt(角色Profile + 环节 + 全部memory + 任务说明)
        speech = await LLM(prompt)
        memory.push(speech)
        前端接收 → 打字机播放 → 微反应引擎更新情绪
```

---

## 情绪 + 跑票系统（双层架构）

### 第一层：前端微反应引擎（不调 LLM）

辩手发言逐字推送到前端时，按句子拆分。每句一到，提取 10 个形态特征（是反问句？是长句？有破折号？有情绪词？……），然后 30 个观众各自过自己的人设滤镜：

```
30 个观众 × 10 个形态特征 × 个人偏好权重 = 300 次乘法 + 300 次加法
纯前端 JS，每句 < 1 毫秒
```

不同观众对同一句有不同的"过敏程度"——程序员对情绪词免疫，大学生对反问句敏感。

产出：前端侧边栏实时情绪分布（😂7 人 / 🤔5 人 / 😤4 人 / 😐14 人）。

### 第二层：段落级深度分析（调 LLM，可选）

V1 暂不启用每段发言后的 LLM 情绪分析。V1 只用前端微反应引擎。

V2 可加：每段完整发言后 → LLM 分析一次情绪配方 → 30 观众过滤镜 → leaning 微调。

### 实时跑票追踪

与情绪面板共享底层数据。每个观众维护一个 `leaning` 值（-10=坚定正方, 0=中立, +10=坚定反方）。前端每句话更新情绪的同时，根据情绪强度微调 leaning。

前端显示实时跑票曲线：
```
正方 ████████████░░░░░░░░  12  ← 初投
     █████████████░░░░░░░  13  ← 正方一辩 +1
     ██████████████░░░░░░  14  ← 正方二辩 +1
     █████████████████░░░  17  ← 终投
```

---

## LLM 调用概览（一集）

| 调用 | 次数 | 类型 |
|------|------|------|
| 辩手发言（6 人） | 6 次 | 必选 |
| 开杠（6 轮） | 6 次 | 必选 |
| 结辩（2 人，三辩兼任） | 2 次 | 必选 |
| 主持人串场 | ~5 次 | 必选 |
| 初投（30 观众，分批并行，每批 5 人） | 30 次 | 必选 |
| 终投（30 观众，分批并行，每批 5 人） | 30 次 | 必选 |
| 导师点评（2 人） | 2 次 | 必选 |
| 每段情绪分析 LLM | ~15 次 | **V2 启用** |
| **合计（V1）** | **~81 次** | |
| **合计（V2）** | **~96 次** | |

> 注：观众 30 人的投票调用是 30 次独立的 LLM 请求（不是 1 次），分批并行发出以减少等待时间。

---

## 辩题处理

**正反方自动拆解**：系统根据关键词自动判定双方立场。

- 辩题含"该不该/要不要/能不能/是不是" → 正方=该/要/能/是，反方=不该/不要/不能/不是
- 辩题含"VS/还是/要么" → 按分隔符拆为两方
- 特殊辩题（无法自动拆解的）→ 在 `topics.json` 中手动标注 `pro_position` / `con_position`

用户不需要操心正反方立场，系统全自动处理。

---

## 播放模式

**默认自动播放**：每段发言结束后，自动延时 0.8 秒请求下一段。

**暂停按钮**：辩论展示区顶部有一个「⏸️ 暂停 / ▶️ 继续」按钮。暂停后，前端停止轮询 `/next`，当前打字机效果也暂停。点继续后恢复。

页面刷新后，自动恢复到暂停前的进度。

---

## 情绪引擎详细算法

### 形态特征集（10 个）

前端按标点符号拆分句子，每句一到立刻提取以下特征（正则匹配，< 1ms）：

| # | 特征名 | 检测规则 |
|---|--------|---------|
| 1 | `is_question` | 以「？/吗/呢」结尾，含「难道/怎么/你告诉」|
| 2 | `has_exclamation` | 含「！」|
| 3 | `has_dash` | 含「——」或「…」（酝酿转折）|
| 4 | `is_short` | 句子 < 10 字（突袭感） |
| 5 | `is_long` | 句子 > 30 字（论证感） |
| 6 | `has_emotion` | 含情绪词：哭/笑/气/怕/爱/恨/感动/伤心/委屈 |
| 7 | `has_life` | 含生活词：买菜/带娃/闺蜜/老公/老婆/吃饭/睡觉/搬砖/上班 |
| 8 | `has_data` | 含数据词：%/数据/统计/研究表明/数据显示/根据/概率 |
| 9 | `has_attack` | 含攻击词：你错了/荒谬/可笑/胡扯/你告诉我/说真的/我问你 |
| 10 | `has_sublime` | 含升华词：人生/意义/价值/未来/梦想/自由/家/爱 |

每个特征值为 0（未触发）或 1（触发）。

### 30 观众过滤镜

每个观众有 10 个敏感度权重（-1.0 ~ +1.0），已定义在 `audience.json` 的 `emotion` 字段：
- 正值 = 对这种特征敏感/喜欢
- 负值 = 对这种特征反感/免疫
- 0 = 无感

### 情绪总分

```
score = sum( feature_i × sensitivity_i ) for i = 1..10
score 归一化到 [-1, +1]
```

### Emoji 映射

```
score > 0.5 + humor/life 特征主导     → 😂 笑了
score > 0.5 + emotion 特征主导       → 🥺 触动
score > 0.5 + attack 特征主导        → 😤 不服
score > 0.5 + logic/sublime 特征主导  → 👏 认同
score > 0.4                          → 🤔 思考
-0.3 < score < 0.3                   → 😐 无感
score < -0.3                         → 😑 无聊
```

主导判定 = 取该句触发特征中 weight 最大的观众敏感度所对应的类别。

### Leaning 变化（跑票公式）

每个观众有 `leaning` 值（-10 = 坚定正方，0 = 中立，+10 = 坚定反方）。

```
if 观众 leaning < 0（倾向正方）:
    if 反方发言 && 观众情绪正面（score > 0.3）:
        leaning += |score| × 0.3   // 被反方打动了，往中立走
    if 正方发言 && 观众情绪正面:
        leaning -= |score| × 0.1   // 正方巩固，微小强化（最多回到 -10）

if 观众 leaning > 0（倾向反方）:
    if 正方发言 && 观众情绪正面:
        leaning -= |score| × 0.3   // 被正方打动了，往中立走
    if 反方发言 && 观众情绪正面:
        leaning += |score| × 0.1   // 反方巩固
```

每段完整发言结束后，取该段所有句子的情绪分数平均值作为"段落级影响"。若平均值 > 0.3，执行 leaning 调整。

初投时确立的初始 leaning（初投正 = -5，初投反 = +5）作为跑票计算的基础。

---

## 环境配置

DeepSeek API Key 通过 `.env` 文件配置：

```
# .env（不提交到 Git）
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
```

提供 `.env.example` 模板文件。后端使用 `dotenv` 包自动加载。

---

## LLM 失败降级方案

### 辩手/结辩发言失败

```
第 1 次调用失败 → 等 2 秒 → 重试
第 2 次重试失败 → 等 2 秒 → 重试
第 3 次重试失败 → 降级：去掉风格约束，用简化 Prompt（只保留立场 + 辩题 + 环节）
降级失败        → 返回占位文本：「（辩论暂时中断...）」
                前端显示 Toast + 「跳过此发言」按钮，不中断整场
```

### 观众投票失败

```
单个观众调 LLM 失败（3 次重试后）：
  → 给默认票：根据该观众的人设倾向投票（倾向正方的投正方，倾向反方的投反方）
  → 标记为"非有效票"

超过 10 个观众失败：
  → 终止本轮投票
  → 结果显示"本投票有效票数 X/30，部分观众未能参与"
```

### 主持人/导师失败

```
3 次重试失败 → 跳过该环节，使用静态备选文案
```

---

## 第一步：搭建基础环境

**做什么**：创建项目目录和骨架文件。

**需要什么**：无。

**产出**：

```
AI的夏天/
├── index.html              ← 节目主页面
├── server.js               ← Node.js 后端（编排器）
├── data/
│   ├── topics.json          ← 辩题库
│   ├── debaters.json        ← 辩手风格库（≥9 个风格）
│   ├── mentors.json         ← 导师风格库
│   ├── referee.json         ← 主持人风格
│   └── audience.json        ← 30 个观众人设（含情绪响应矩阵）
├── package.json             ← Node 依赖
└── start.sh                 ← 一键启动脚本
```

**做完看到什么**：`npm start` → 浏览器打开 `localhost:3000`，看到空白骨架页面。

---

## 第二步：编写角色风格库

**做什么**：为每个角色写好"说话说明书"及观众人设。

**需要什么**：你对《奇葩说》辩手/导师/主持人的了解。

**产出**：

- **辩手风格** `data/debaters.json`：≥9 个（6 个辩位至少需要 6 种不同风格，常用阵营有备选）
- **主持人风格** `data/referee.json`：1 个（马东式幽默控场）
- **导师风格** `data/mentors.json`：2 个（固定：薛兆丰经济学视角 + 蔡康永温柔人文）
- **观众人设** `data/audience.json`：30 个，每个包含：
  - 人设标签、判断维度、情感倾向
  - **情绪响应矩阵**（humor_affinity / logic_affinity / emotion_affinity / story_affinity / aggression_tolerance / novelty_seeking / golden_line_sensitivity）
  - **主题触发词**（这个观众对什么话题特别敏感）

**做完看到什么**：4 个 JSON 文件，定义了整场节目的所有角色画像。

---

## 第三步：制作辩论引擎 + 后端服务

**做什么**：写 `server.js`，实现编排器 + 状态机 + Prompt 工厂 + API 路由。

**需要什么**：所有风格数据（第二步产出）、辩题库、DeepSeek API key。

**API 路由**：

| 路由 | 功能 |
|------|------|
| `POST /api/session/start` | 创建辩论 session，返回 session_id |
| `GET /api/session/:id/state` | 获取当前状态（恢复用） |
| `GET /api/session/:id/next` | 执行下一步，返回下一段发言 |
| `GET /api/session/:id/full` | 获取完整记录（导出用） |

**核心模块**：

| 模块 | 职责 |
|------|------|
| `sessionStore` | 内存存储 session 状态（step / memory / viewerStates） |
| `flowEngine` | 状态机，按 FLOW 数组推进 |
| `promptBuilder` | 根据角色 + 风格 + 环节 + 上下文组装 system prompt |
| `agentCaller` | 调 DeepSeek API，含重试（失败等 2s 重试，最多 3 次） |
| `audienceBatch` | 30 观众分批并发（每批 5 人） |

**前端沟通方式**：前端循环调用 `/api/session/:id/next`，每次拿到一段新发言 → 打字机播放 + 微反应更新 → 再请求下一段。辩论结束时 `/next` 返回 `{ done: true }`。

**做完看到什么**：终端里 `curl` 能从头到尾拿到完整辩论文字。

---

## 第四步：制作主持人系统

切入 `promptBuilder`，根据 `/data/referee.json` 在对应环节（开场、环节过渡、开杠喊停、引导投票、引导导师）生成主持人的串场语。

---

## 第五步：制作导师点评系统

辩论结束后，将完整辩论记录 + 导师风格说明书喂给 LLM，输出 150-300 字点评。2 位导师顺序点评（薛兆丰 → 蔡康永）。

---

## 第六步：搭建前端界面

**做什么**：把所有后端逻辑变成好看、有动画的网页。

**需要什么**：前五步所有产出。

**前端结构**：

```
阵容配置区
  ├── 辩题选择（预设题库 + 自定义输入）
  ├── 辩手阵容（正方一/二/三 + 反方一/二/三，各带风格下拉，V1 不限制风格搭配）
  ├── 主持人 + 导师选择（导师固定 2 人，不可改数量）
  └── [开始辩论] 按钮

辩论展示区（运行时按序展开）
  ├── ⏸️ 暂停/继续 按钮
  ├── 主持人开场
  ├── 初始投票（条形图动画）
  ├── 逐辩手发言（打字机效果，左右气泡交替）
  ├── 侧边栏：实时情绪分布（😂/🤔/😤/😐）
  ├── 侧边栏：实时跑票曲线
  ├── 开杠区（左右对战模式，交替高亮）
  ├── 结辩区
  ├── 最终投票（初投→终投过渡动画 + 跑票理由卡片）
  ├── 导师点评区（卡片式）
  └── [再来一局] [换阵容] [导出台本]

异常处理
  ├── LLM 调用失败 → Toast 提示 + 自动重试（3 次）
  │     └── 降级：去掉风格约束重试 → 仍失败：占位文本 + 「跳过」按钮
  ├── 观众投票失败 → 默认票（按人设倾向）→ 超过 10 人失败则标注
  ├── 用户刷新页面 → 自动恢复当前进度 + 情绪/跑票数据
  └── 网络断开 → 暂停 + 提示重连
```

**关键技术点**：
- 打字机逐字输出（setInterval 逐字追加到 DOM）
- 微反应引擎（每句一到立刻算 30 观众情绪 + 更新侧边栏）
- `session/state` 接口实现刷新恢复
- 纯前端实现（无 React/Vue，零框架依赖）

**做完看到什么**：一个完整的、可以在浏览器里"播放"辩论过程的节目。

---

## 第七步：风格校准（使用音频）

**做什么**：用真实辩手音频，让 AI 风格模拟更精准。

**需要什么**：你提供的辩手音频（每人 3-5 段，每段 1-3 分钟）。

**流程**：音频 → 转写文字 → 分析句式/比喻/节奏/口头禅 → 更新 `debaters.json`。

---

## 第八步（二期）：TTS 语音版

**做什么**：把文字辩论变成音频播客。

**流程**：文字台本 → 每段匹配 TTS 音色 → 逐段合成 → 拼接 + 音效 → 导出 MP3。

---

## 制作顺序总览

```
第一步 ─→ 骨架文件（空项目）
第二步 ─→ 角色说明书（4 个 JSON）
第三步 ─→ 辩论引擎 + 后端服务（能跑完整辩论）
第四步 ─→ 主持人系统（有节目感）
第五步 ─→ 导师点评（不同视角）
第六步 ─→ 前端界面（好看能看，含情绪+跑票）
第七步 ─→ 风格校准（用音频优化）
第八步 ─→ 语音版（播客）
```

**最短可用版本**：做完第六步，就是一个完整的文字辩论节目。
