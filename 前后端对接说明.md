# 前后端对接说明 — 后端→前端

> 后端已就绪（`server/server.js`），前端已就绪（`index.html`），这份文档告诉你怎么把两个人牵上线。

---

## 1. 启动后端

```bash
cd /Users/Andy/Desktop/AI的夏天
npm start
# → 后端运行在 http://localhost:3000
```

`index.html` 也在项目根目录，Express 会把它作为静态文件托管，所以打开 `http://localhost:3000` 就能同时看到前端和后端。

---

## 2. 前端需要改的代码

### 2.1 API 基础地址

前端所有 fetch 请求都指向 `http://localhost:3000`：

```javascript
const API_BASE = 'http://localhost:3000';
```

### 2.2 交互流程（伪代码）

```javascript
// 1. 用户点击「开始辩论」→ 创建 Session
const startRes = await fetch(`${API_BASE}/api/session/start`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    pro: ['chenming', 'fushouer', 'huangzhizhong'],  // 正方3人
    con: ['lidan', 'zhanqingyun', 'maweiwei'],         // 反方3人
    topicIndex: 0  // 辩题索引（0-19），不传则随机
  })
});
const { data: { sessionId } } = await startRes.json();

// 2. 循环调 /next 播完整场辩论
let done = false;
while (!done) {
  const nextRes = await fetch(`${API_BASE}/api/session/${sessionId}/next`);
  const { data } = await nextRes.json();

  if (data.done) {
    done = true;
    break;
  }

  // 根据 data.type 决定渲染方式
  switch (data.type) {
    case 'host':    renderHost(data); break;
    case 'speech':  renderSpeech(data); break;
    case 'battle':  renderBattle(data); break;
    case 'closing':  renderClosing(data); break;
    case 'vote':    renderVote(data); break;
    case 'mentor':  renderMentor(data); break;
  }

  await waitForTypingEffect();  // 打字机播完再去拿下一段
}

// 3. 辩论结束 → 显示「再来一局」「导出台本」

// 4. 页面刷新 → 调 /state 恢复进度
const stateRes = await fetch(`${API_BASE}/api/session/${sessionId}/state`);
const { data: state } = await stateRes.json();
// state.currentStepIdx → 恢复进度
// state.memoryLength → 已经播了多少段
```

---

## 3. API 响应格式对照（前端照着解析）

### POST /api/session/start

```json
// 前端传参（阵容选择页面的数据）
{ "pro": ["chenming","fushouer","huangzhizhong"], "con": ["lidan","zhanqingyun","maweiwei"] }

// 后端返回
{ "code": 200, "data": { "sessionId": "7a8e2f46-c34c-4d1d-b2bf-24a85ab2b5e3" }, "message": "ok" }
```

### GET /api/session/:id/state（刷新恢复用）

```json
{
  "code": 200,
  "data": {
    "sessionId": "7a8e2f46-...",
    "topicTitle": "结婚到底要不要买房？",
    "proTeam": [
      { "id": "chenming", "name": "陈铭风格", "label": "逻辑型", "assignedPosition": "first" },
      { "id": "fushouer",  "name": "傅首尔风格", "label": "金句型", "assignedPosition": "second" },
      { "id": "huangzhizhong", "name": "黄执中风格", "label": "煽情型", "assignedPosition": "third" }
    ],
    "conTeam": [
      { "id": "lidan", "name": "李诞风格", "label": "解构型", "assignedPosition": "first" },
      ...
    ],
    "status": "RUNNING",
    "currentStepIdx": 5,
    "currentStepName": "pro_second",
    "memoryLength": 5,
    "overtimeRounds": 0
  }
}
```

### GET /api/session/:id/next（核心！前端按 type 分流）

#### 🎤 type: "host" — 主持人发言

```json
{
  "step": "host_opening",
  "type": "host",
  "content": "各位亲爱的观众朋友们，欢迎来到今天的《奇葩说》！...",
  "side": null,
  "speakerName": "马东",
  "degraded": false,
  "attemptCount": 1,
  "flowProgress": { "current": 1, "total": 26 }
}
```
> 前端渲染：居中、斜体、打字机效果，发言人标"马东"

#### 🎤 type: "speech" — 辩手发言（一辩/二辩/三辩）

```json
{
  "step": "pro_first",
  "type": "speech",
  "content": "这道题的关键不在于要不要买房，而在于...",
  "side": "pro",
  "speakerName": "陈铭风格",
  "degraded": false,
  "attemptCount": 1,
  "flowProgress": { "current": 4, "total": 26 }
}
```
> 前端渲染：左右气泡（pro左侧、con右侧），打字机输出，触发微反应引擎

#### ⚔️ type: "battle" — 开杠

```json
{
  "step": "battle_r1_pro",
  "type": "battle",
  "content": "我刚才说的是...",
  "side": "pro",
  "speakerName": "黄执中风格",
  "flowProgress": { "current": 13, "total": 26 }
}
```
> 前端渲染：对战面板，当前发言者高亮，对方变暗

#### 🏁 type: "closing" — 结辩

```json
{
  "step": "con_closing",
  "type": "closing",
  "content": "今天的辩论到了最后...",
  "side": "con",
  "speakerName": "马薇薇风格",
  "flowProgress": { "current": 20, "total": 26 }
}
```

#### 📊 type: "vote" — 投票结果

```json
{
  "step": "vote_init",
  "type": "vote",
  "label": "init",          // "init"=初投, "final"=终投
  "pro": 11,
  "con": 18,
  "abstain": 1,
  "pro_pct": 38,            // 可以直接给条形图用
  "con_pct": 62,
  "validCount": 29,
  "details": [
    {
      "viewerId": 1,
      "label": "职场妈妈",
      "choice": "pro",
      "reason": "房子是稳定生活的基石，给孩子和家庭安全感",
      "confidence": 0.85
    },
    ...
  ],
  "flowProgress": { "current": 2, "total": 26 }
}
```
> **前端处理**：
> - `pro_pct` / `con_pct` → 画条形图
> - 初投结果先存着，终投出来后做过渡动画（初投→终投对比）
> - 终投时计算跑票人数（终投pro - 初投pro = 正方净增票数）
> - `details` 里每位观众的 `reason` → 跑票揭秘卡片

#### 🎙️ type: "mentor" — 导师点评

```json
{
  "step": "mentor_1",
  "type": "mentor",
  "content": "从经济学视角来看，这并不是一个对错的问题...",
  "side": null,
  "speakerName": "薛兆丰风格",
  "flowProgress": { "current": 25, "total": 26 }
}
```
> 前端渲染：导师卡片（不同导师不同头像/配色）

#### 🏁 辩论结束

```json
{
  "done": true,
  "status": "SUCCESS"   // 或 "ABORTED"
}
```

---

## 4. 前端需要自己实现的部分

这些是**前端微反应引擎**的工作，后端只提供数据，不动这些：

| 前端功能 | 数据来源 | 实现方式 |
|---------|---------|---------|
| **打字机逐字效果** | `data.content` | setInterval 逐字追加到 DOM |
| **情绪面板**（😂/🤔/😤/😐） | `data/audience.json` | 前端微反应引擎（按句子提取10个形态特征） |
| **实时跑票曲线** | 前端自己算 | 观众 leaning 值 + 情绪强度 → 实时更新 |
| **暂停/继续** | 前端控制轮询 | 暂停就不调 `/next`，继续就恢复调 |
| **页面刷新恢复** | `GET /state` | 记录 sessionId 到 localStorage，刷新后重新 fetch |
| **跑票理由卡片** | vote 的 `details[].reason` | 终投完成后，对比初投/终投展示跑票观众的理由 |

---

## 5. 顺便能用的静态资源

| URL | 内容 | 前端用途 |
|-----|------|---------|
| `GET /data/topics.json` | 20道辩题 | 辩题选择下拉框 |
| `GET /data/debaters.json` | 12个辩手 | 阵容选择页面的选项 |
| `GET /data/audience.json` | 30个观众 | 微反应引擎的底层数据 |
| `GET /data/referee.json` | 马东风格 | (可选)主持人信息 |
| `GET /data/mentors.json` | 薛兆丰+蔡康永 | (可选)导师信息 |

---

## 6. 一句话总结

> 🎯 **前端只需做这一件事：循环调 `/api/session/{id}/next`，每次拿到一段数据，看 `type` 字段决定怎么渲染。情绪和跑票前端自己算。**
