# AI 奇葩说 — 项目总览

> 一个模仿《奇葩说》的 AI 辩论节目。两个 AI 阵容各派一辩/二辩/三辩，300 个有独立人设的 AI 观众池（每场随机抽 30 人）实时投票跑票，导师赛后点评。全自动 Loop 驱动。

---

## ⚠️ 每个 Agent 必须先读这个

**这是项目的全局看板。** 任何 Agent 进入项目后：

1. **先看进度表**（↓下面）— 了解哪些任务做完了、哪些在做、哪些没开始
2. **执行自己的任务** — 读完任务文件，按 Loop 协议干活
3. **干完后立即更新进度表** — 把自己的任务状态改成 `✅ 完成` 或 `❌ 失败`
4. **在日志区写记录** — 做了什么、产出在哪、遇到了什么问题

**如果一个任务的依赖还没完成，你就不该开始那个任务。**

---

## 项目进度看板

> 最后更新：2026-07-28

| 任务 | Agent | 状态 | 产出物 | 备注 |
|------|-------|------|--------|------|
| **任务 A** — 辩手风格研究 | Agent 1 | ✅ | `data/debaters.json` | 12 人完整风格库 |
| **任务 B** — 主持人与导师研究 | Agent 2 | ✅ | `data/referee.json` + `data/mentors.json` | 马东+薛兆丰+蔡康永+李诞 |
| **任务 E** — 数据与配置 | Agent 3 | ✅ | `data/topics.json` 等 7 个文件 | 20题+30观众骨架+配置 |
| **任务 C** — 后端核心搭建 | Agent 4 | ✅ | `server/` 目录（5模块） | 105项测试全通过 |
| **任务 D** — 前端界面搭建 | Agent 5 | ✅ | `index.html` | 已就绪 |
| **观众池升级** — 300人 | — | ✅ | `data/audience_300.json` | 每人1000+字人生故事，每场随机抽30人 |
| **音视频处理** | Agent 6 | ⬜ 未开始 | `materials/` 目录 | ⚠️ 等用户提供链接 |

**状态说明**：⬜ 未开始 | 🔄 进行中 | ✅ 完成 | ⚠️ 降级完成（有部分缺失）| ❌ 失败（见日志）

### Phase 启动条件

| Phase | 启动条件 | 包含任务 |
|-------|---------|---------|
| **Phase 1** | 随时可启动（互相独立） | A、B、E |
| **Phase 2** | A ✅ 且 B ✅ 且 E ✅ | C、D |
| **Phase 2b** | C ✅ | 观众池升级（300人生成） |
| **Phase 3** | C ✅ 且 D ✅ | 联调测试 |
| **Phase 4** | 等用户 | 音视频 |

---

## 工作日志

> Agent 完成任务后，在下面追加一条。格式：`[日期] [Agent] [结果] 一句话总结 + 产出文件路径`

- `[2026-07-28] [团队] [✅] 后端核心搭建完成，server/目录5模块~1350行代码，105项测试全通过`
- `[2026-07-28] [团队] [✅] 300人观众池上线，data/audience_300.json，59种职业×20城市，每人平均1637字人生故事`
- `[2026-07-28] [团队] [✅] 模型从deepseek-chat切换为deepseek-v4-flash，Base URL去掉/v1后缀`

---

## 项目文档索引

| 文档 | 类型 | 用途 |
|------|------|------|
| `项目总览.md` | 入口 | **本文档。** 新 Agent 先读这个 |
| `制作步骤.md` | 设计 | 8 步制作计划，每一步的输入/输出 |
| `要达到的效果.md` | 设计 | 最终产品长什么样，逐幕效果描述 |
| `Loop设计.md` | 架构 | Loop Engineering 范式，目标/过程/自检/停止条件 |
| `任务_A_辩手风格研究.md` | 任务 | 12 个辩手的深度学习研究 |
| `任务_B_主持人与导师研究.md` | 任务 | 主持人+2 位导师的风格研究 |
| `任务_C_后端核心搭建.md` | 任务 | Node.js 后端，含 FLOW 定义和 API 规格 |
| `任务_D_前端界面搭建.md` | 任务 | 单文件 HTML 前端，含情绪面板+跑票 |
| `任务_E_数据与配置.md` | 任务 | 静态数据文件和项目配置 |
| 计划文件（`~/.workbuddy/plans/...`） | 参考 | 音视频素材处理方案 |

---

## 技术概览

- **前端**：纯 HTML+CSS+JS，无框架依赖
- **后端**：Node.js + Express，编排器+状态机+Prompt 工厂
- **LLM**：DeepSeek API（OpenAI 兼容格式），`deepseek-v4-flash` 模型
- **API 地址**：`https://api.deepseek.com`（不带 /v1 后缀）
- **部署**：V1 本地 localhost:3000，V2 上云
- **架构范式**：Loop Engineering（Task Loop 模式）

---

## 角色体系

| 角色 | 人数 | 说明 |
|------|------|------|
| 正方一辩/二辩/三辩 | 3 | 一辩立论、二辩补充、三辩攻防+结辩 |
| 反方一辩/二辩/三辩 | 3 | 同上 |
| 主持人 | 1 | 控场控节奏，马东风格 |
| 导师 | 4 | 薛兆丰+蔡康永+刘擎+李诞，赛后点评不打分 |
| AI 观众 | **300 人池，每场随机抽 30 人** | 每人有1000+字人生故事+情绪矩阵+投票偏好，初投/终投投票 |

### 辩手风格库（12 人）

陈铭、傅首尔、肖骁、黄执中、詹青云、李诞、颜如晶、马薇薇、邱晨、姜思达、席瑞、范湉湉

---

## 建议的 Agent 分配

### Phase 1：基础数据 + 素材研究（3 个 Agent，可全部并行）

```
┌─ Agent 1 ──┐   ┌─ Agent 2 ──┐   ┌─ Agent 3 ──┐
│ 任务 A      │   │ 任务 B      │   │ 任务 E      │
│             │   │             │   │             │
│ 搜索12个     │   │ 搜索马东     │   │ topics.json │
│ 辩手的金句   │   │ 薛兆丰      │   │ audience    │
│ 风格分析     │   │ 蔡康永      │   │ .json(30人) │
│ 弱点/禁忌    │   │ 的经典点评  │   │ package.json│
│             │   │ 主持风格    │   │ .env模板    │
│             │   │             │   │ debaters    │
│             │   │             │   │ 骨架文件    │
│ → debaters  │   │ → referee   │   │             │
│    .json    │   │   mentors   │   │             │
│             │   │   .json     │   │             │
└─────────────┘   └─────────────┘   └─────────────┘
       │                 │                  │
       └─────────┬───────┴──────────────────┘
                 ↓
         【所有 JSON 数据就绪】
```

**Agent 1**：执行 `任务_A_辩手风格研究.md`，产出 `data/debaters.json`
- 先读 `制作步骤.md` 了解辩手风格库的需求
- 搜索 12 个辩手的金句、风格分析、辩论片段
- 按任务 A 中的格式产出完整 JSON

**Agent 2**：执行 `任务_B_主持人与导师研究.md`，产出 `data/referee.json` + `data/mentors.json`
- 搜索马东的串场风格、薛兆丰的经典点评、蔡康永的经典语录
- 按任务 B 中的格式产出 JSON

**Agent 3**：执行 `任务_E_数据与配置.md`
- 创建 `data/topics.json`（15-20 题）
- 创建 `data/audience.json`（30 人，含完整的情绪响应矩阵）
- 创建 `data/debaters.json` 骨架（Agent 1 会填充）
- 创建 `data/referee.json` 和 `data/mentors.json` 骨架（Agent 2 会填充）
- 创建 `package.json` 和 `.env.example`

### Phase 2：系统搭建（2 个 Agent，可并行，依赖 Phase 1 产出的 JSON）

```
┌─ Agent 4 ──┐   ┌─ Agent 5 ────────┐
│ 任务 C      │   │ 任务 D           │
│             │   │                  │
│ server.js   │   │ index.html       │
│ 状态机      │   │ 辩论 UI          │
│ Prompt工厂  │   │ 打字机效果        │
│ LLM调用     │   │ 情绪面板(侧边栏)  │
│ 观众引擎    │   │ 跑票曲线          │
│ 4个API路由  │   │ 暂停/恢复/刷新    │
│ Loop自检    │   │ 投票动画          │
│             │   │ 导师卡片          │
└─────────────┘   └──────────────────┘
       │                  │
       └────────┬─────────┘
                ↓
        【联调测试】
```

**Agent 4**：执行 `任务_C_后端核心搭建.md`，产出 `server/` 目录
- 先读 `Loop设计.md` 了解 Loop 机制
- 先读 Phase 1 产出的所有 JSON 文件（特别是辩手/主持人/导师格式）
- 实现任务 C 中的所有步骤

**Agent 5**：执行 `任务_D_前端界面搭建.md`，产出 `index.html`
- 先读 `要达到的效果.md` 了解 UI 期望
- 先读 `制作步骤.md` 了解情绪引擎算法
- 实现任务 D 中的所有步骤
- 开发期间可用 mock 数据，不依赖后端

### Phase 3：音视频素材处理（需要用户提供链接后执行）

**Agent 6**（或复用 Agent 1）：执行音视频处理方案
- 安装依赖（yt-dlp + whisper）
- 下载音频 → 转写 → 时间聚类 → 产出匹配表
- **用户介入**：做说话人匹配
- 按辩手归类发言 → 更新 `data/debaters.json` 的 `example_lines`

---

## Agent Loop 协议（每个 Agent 必须遵守）

你不是在执行一个任务清单。你是在运行一个 **Task Loop**。

### 你的 Loop 结构

```
┌────────────────────────────────────────────┐
│  LOOP: 任务名                                │
│                                            │
│  ① 读项目总览.md → 看进度看板 → 确认可开始   │
│  ② 读自己的任务文件 → 理解目标/步骤/自检规则   │
│     ↓                                      │
│  ③ 更新进度看板：状态 → 🔄 进行中            │
│  ④ 执行一步（任务文件 §3）                   │
│  ⑤ 自检（任务文件 §4 的检查项）               │
│     ├── 通过 → 继续下一步                     │
│     └── 不通过 → 重试（最多 3 次）            │
│         ├── 3 次全部失败 → 降级              │
│         └── 降级仍失败 → 停止                │
│     ↓                                      │
│  ⑥ 全部完成 + 终检通过                       │
│     ↓                                      │
│  ⑦ 更新进度看板：状态 → ✅ / ⚠️ / ❌         │
│  ⑧ 在工作日志区追加一条记录                   │
└────────────────────────────────────────────┘
```

### 你必须在每一步之后自问

1. **我这一步的产出物存在吗？**（文件是否已写、JSON 是否合法）
2. **产出物符合规格吗？**（字段名对不对、格式对不对、数量够不够）
3. **有没有漏掉什么？**（任务文件 §4 的 checklist 每一项都过一遍）

### 遇到失败怎么办

```
第 1 次失败：换一个方式重试（不重复同样的操作）
第 2 次失败：再次换方式
第 3 次失败：降级——降低要求或换一条路达到目标
降级也失败：停止。写到任务文件的"失败报告"区域，标注：
  - 卡在哪一步
  - 试了什么
  - 最后一次失败的原因
```

### 不要做的事情

- 不要跳过自检："差不多行了" = 不合格
- 不要猜：不确定就问用户，不要自己编数据
- 不要忘记存文件：每完成一个子目标就写盘，不要攒到最后

---

## Agent 开场指令模板

```
你是 [Agent 角色]，负责执行 [任务文件名]。

你的工作目录：/Users/Andy/Desktop/AI的夏天/

你必须遵守以下流程：

1. 先读 项目总览.md 的"项目进度看板"
   → 确认你的任务没有被别人做完
   → 确认依赖条件已满足（Phase 启动条件）
   
2. 把你的任务状态改为 🔄 进行中

3. 读你的任务文件，按 Loop 方式逐步执行
   每步之后自检，失败则重试/降级

4. 完成任务后：
   → 更新进度看板（✅ / ⚠️ / ❌）
   → 在工作日志区追加记录
```


---

## 执行顺序总结

```
Phase 1（并行）
  Agent 1 ─ 任务 A ─→ data/debaters.json
  Agent 2 ─ 任务 B ─→ data/referee.json + data/mentors.json  
  Agent 3 ─ 任务 E ─→ topics.json, audience.json, 配置文件
        │
        ▼ 等待全部完成
Phase 2（并行）
  Agent 4 ─ 任务 C ─→ server/ 目录（需要 Phase 1 产出的 JSON）
  Agent 5 ─ 任务 D ─→ index.html（开发期可用 mock 数据）
        │
        ▼
Phase 2b — 观众池升级
  ─→ data/audience_300.json（300人，每人1000+字人生故事）
  ─→ 后端适配：每场随机抽30人，bio注入投票Prompt
        │
        ▼ 全部完成
Phase 3 — 联调 + 测试
Phase 4（等你的链接）— 音视频处理 ─→ 更新 debaters.json
```

---

## 📌 实时状态

当前项目的最新状态（架构决策、变更记录、TODO）请参考 `.workbuddy/memory/MEMORY.md`
